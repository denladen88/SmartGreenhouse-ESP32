# Expo version

This project pins `expo ^54.0.0` (see `package.json`). Read the exact versioned
docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — the API
surface shifts between SDKs.
